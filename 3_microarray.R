#-------------------------------------------------------------------------------
# PROGRAMMING WORKSHOP - DAY 2: R
#-------------------------------------------------------------------------------
# DATA FRAMES
# * reading / writing data frames
# * subsetting data frames
#-------------------------------------------------------------------------------
setwd("~/Dropbox/SGUL_Teaching/SGUL_Workshops/2017_Programming_Workshop")
list.files()
df <- read.table("microarray_data.txt", header=T, sep="\t")
class(df)
dim(df)
nrow(df)
ncol(df)
head(df)
names(df)


#-------------------------------------------------------------------------------
# Subsetting
#-------------------------------------------------------------------------------
df2 <- subset(df, logFC > 2)
nrow(df2)
head(df2)

#-------------------------------------------------------------------------------
# Saving a data frame:
#-------------------------------------------------------------------------------
write.table(df2, file="new_table.txt", sep="\t", row.names=FALSE, col.names=TRUE)

#-------------------------------------------------------------------------------
# More subsetting ...
#-------------------------------------------------------------------------------


